pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        // এখানে Mozilla-র রিপোজিটরি যোগ করা হলো
        maven { url 'https://maven.mozilla.org/maven2/' }
    }
}

rootProject.name = "PureWeb"
include ':app'